# define defualt shell
shell = "/usr/bin/zsh"

# imports IMP: Don't move this line anywhere else, or don't put anything above this.
import = [
  "~/.config/alacritty/catppuccin-mocha.toml",
  "~/.config/alacritty/font_config.toml",
]

[env]
TERM = "xterm-256color"

[window.dimensions]
columns = 80
lines = 20

[window]
dynamic_padding = true
